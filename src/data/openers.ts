export type OpenerFn = (title: string) => string;
export type OpenerSet = [OpenerFn, OpenerFn, OpenerFn];

export const openerSuggestionSets: OpenerSet[] = [
  [
    (title) => `「我先说一个判断：${title}——真正值得讨论的不是时间点，而是它正在改变哪些具体工作。」`,
    (title) => `「如果只看表面现象，「${title}」很容易被带偏。我更想从实际落地和用户感知两个角度拆开看。」`,
    (title) => `「作为长期关注这一领域的人，我觉得「${title}」的关键不在于某个节点，而是能力边界正在怎样被改写。」`,
  ],
  [
    (title) => `「${title}——这个问题最好不要从技术名词讲起，而要先回答：它会影响谁的日常工作？」`,
    (title) => `「我会把「${title}」拆成三个层次：工具替代、流程重组，以及岗位能力要求的变化。」`,
    (title) => `「很多人担心的是被替代，但围绕「${title}」，真实变化往往先发生在协作方式和决策速度上。」`,
  ],
  [
    (title) => `「先给结论：${title}——短期不会一刀切改变现状，但会迅速拉开会用和不会用的人。」`,
    (title) => `「我更关心「${title}」背后的底层逻辑：这不是新增一个按钮，而是在重写用户完成任务的路径。」`,
    (title) => `「如果你正在做产品或内容，「${title}」这个问题的答案会直接影响接下来半年该补什么能力。」`,
  ],
];

export const defaultOpeners = [
  '「上周和某大厂 AI 负责人聊了 2 小时，他说的一句话让我重新理解了 AGI 进程……」',
  '「作为在 AI 领域摸爬滚打 5 年的从业者，我想用一个反直觉的视角来回答这个问题——」',
  '「先抛结论：AGI 不会在你期待的时间节点到来，但它已经在你意想不到的地方出现了。」',
];
